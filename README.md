# MCD123 - Next.js + Auth

带用户名密码登录 + Google 登录的项目

## 快速开始

```bash
npm install
npm run dev
```

## 环境变量
创建 `.env.local` 文件

```env
NEXTAUTH_SECRET=your-secret-key
NEXTAUTH_URL=http://localhost:3000
GOOGLE_CLIENT_ID=your-google-id
GOOGLE_CLIENT_SECRET=your-google-secret
```